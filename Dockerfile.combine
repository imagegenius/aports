# this file needs to be modified to match list of 'ALPINETAG's in the Jenkins matrix

# e.g 'FROM ghcr.io/imagegenius/aports-cache:v<ALPINETAG>-<arch> as <ALPINETAG>-<arch>'
FROM ghcr.io/imagegenius/aports-cache:v3.17-x86_64 as 3.17-x86_64
FROM ghcr.io/imagegenius/aports-cache:v3.17-aarch64 as 3.17-aarch64

FROM scratch

# e.g 'COPY --from=<ALPINETAG>-<arch> / /'
COPY --from=3.17-x86_64 / /
COPY --from=3.17-aarch64 / /